import Dashboard from "@/components/Dashboard";

// This page is the entry point. Dashboard is a client component that
// handles connection state, data loading and tab routing.
export default function Home() {
  return <Dashboard />;
}
