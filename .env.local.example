# Actual Budget HTTP API connection
# The URL of your actual-http-api instance (server-side only, never sent to browser)
ACTUAL_API_URL=http://api-URL:5007
ACTUAL_API_KEY=your-api-key-here

# SQLite database path (defaults to ./data/cashflow.db)
# DB_PATH=./data/cashflow.db